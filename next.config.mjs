/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    swcMinify: true,
    assetPrefix: './',
    output:"export",
    distDir:"/extention/next"
};

export default nextConfig;
